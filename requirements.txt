# Core UI / OpenGL
imgui[glfw]>=2.0       # pyimgui (glfw 통합 extras). 설치 시 PyOpenGL/glfw를 자동으로 가져올 수 있음.
PyOpenGL>=3.1
glfw>=2.0

# Optional: YAML config support
PyYAML>=6.0

# Optional: Global hotkeys for Linux
evdev>=1.5   # Linux-only, 권한/디바이스 접근 필요. (주석 처리 가능)

# Misc (utilities often useful)
setuptools>=65.0